name: Callback Workflow with Matrix per Job

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy (master, master2, master3)'
        required: true
        default: 'master'
  push:
    branches:
      - master


jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    name: A job to say hello
    env:
      BUILD_NUMBER: ${{ github.run_number }}
      
    steps:
      - uses: actions/checkout@v4
      - name: First Composite Action
        run: |
          echo "new upload files" >> new.txt

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifact-${{ env.BUILD_NUMBER }}
          path: ${{ github.run_number }}/dist/p-one-ad-b2c-test/browser
          retention-days: 2

  downlaod:
    runs-on: ubuntu-latest
    name: Download
    env:
      BUILD_NUMBER: ${{ github.run_number }}
      
    steps:
      - uses: actions/checkout@v4

      - name: Download Code Artifacts
        uses: actions/download-artifact@v4
        with:
          name: artifact-${{ env.BUILD_NUMBER }}
          path: /

      - name: List
        run: |
          ls -ltr
          cat new.txt
